export type FormValues = {
    name: string;
    description: string;
    imageURL: string;
    date: Date;
};

export type EditFormValues = {
    id: string;
    name: string;
    description: string;
    imageURL: string;
    date: Date;
};
